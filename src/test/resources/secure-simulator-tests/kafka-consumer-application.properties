# Consumer Bootstrap Server
consumer.bootstrap-servers=SSL://localhost:9090

# Consumer Client ID
consumer.client-id=kafka-consumer

# Consumer Group ID
consumer.group-id=kafka-consumer-group

# Consumer topic
consumer.topic=HCPA

# Security protocol
consumer.security-protocol=SSL

# Truststore location
consumer.truststore-location=classpath:secure-simulator-tests/kafka-broker-keystore.jks

# Truststore password
consumer.truststore-password=ratp123

# Keystore locaton
consumer.keystore-location=classpath:secure-simulator-tests/kafka-broker-keystore.jks

# Keystore password
consumer.keystore-password=ratp123

# Key password
consumer.key-password=ratp123