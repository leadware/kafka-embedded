# Producer Bootstrap Server
producer.bootstrap-servers=SSL://localhost:9090

# Producer Client ID
producer.client-id=kafka-producer

# Producer topic
producer.topic=HCPA

# Security protocol
producer.security-protocol=SSL

# Truststore location
producer.truststore-location=classpath:secure-simulator-tests/kafka-broker-keystore.jks

# Truststore password
producer.truststore-password=ratp123

# Keystore locaton
producer.keystore-location=classpath:secure-simulator-tests/kafka-broker-keystore.jks

# Keystore password
producer.keystore-password=ratp123

# Key password
producer.key-password=ratp123